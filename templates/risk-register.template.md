# 风险登记册 risk-register（P0 起草，agent 填 · 人闸①审）

**唯一集中放领域知识的地方，价值最高。** 先纳入 `redline-library/<品类>.md` + `_generic.md` 的所有条目（除非明显不适用），再按描述增补。

## 失效模式目录
| 编号 | 失效模式（什么会出错·怎么害用户） | 严重度 | 触发条件 | 守护（锁测试，待补） | 状态 |
|---|---|---|---|---|---|
| R1 | 〈来自品类库/描述推断〉 | S3/P0/P1/P2 | 〈何时触发〉 | 〈锁测试名，未建则待补〉 | ❌未护 |

## 规则
- **严重度**：S3/红线（不可逾越，凌驾一切）> P0 > P1 > P2 > P3。
- 新增红线失效模式：**先登记 → 必配锁测试 → 状态置 ✅ 才算闭合**；未护视同 P0/P1。
- 起草问三问：① 这软件**最不能出的错**是什么？② 哪步**算钱/算量/发通知/改状态**错了用户会受害？③ 哪些是**平台/部署/跨端**才暴露的？
- 涉**真实世界外部输入**（OCR/识别/证件）→ 必登记"真实输入未验"项（铁律#12），状态挂 `fixture_debt` 直到有真实样本。

## 完工后
- 把本项目新发现、品类库里没有的红线 → **补回 `redline-library/<品类>.md`**（复利沉淀）。
